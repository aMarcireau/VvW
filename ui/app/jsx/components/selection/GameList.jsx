var React = require('react/addons');

var GameProperties = require('./GameProperties.jsx');

module.exports = React.createClass({
    render: function() {
        return (
            <div className="gameList">
                Hello, world! I am a CommentBox
            </div>
        );
    }
});
